---
- name: This playbook installs apche2 on Ubuntu
  hosts: dbservers
  become: true
  tasks:
  - name: Install Apache2
    apt:
      name: apache2
      state: present
    notify: start apache2

  handlers:
  - name: start apache2
    service:
      name: apache2
      state: started
